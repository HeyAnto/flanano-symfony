<section class="pink-message-container flex">
    <div class="scrolling-text flex gap-50">
        <div class="scrolling-content flex gap-50">
            {% for i in 1..6 %}
            <p>{{ message }}</p>
            {% endfor %}
        </div>
        <div class="scrolling-content flex gap-50" aria-hidden="true">
            {% for i in 1..6 %}
            <p>{{ message }}</p>
            {% endfor %}
        </div>
    </div>
</section>